/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'static.itdg.com.br', 'cdn.bazara.co.mz', 'encrypted-tbn0.gstatic.com', 'cdn.recheio.co.mz'],
  },
};

module.exports = nextConfig;
